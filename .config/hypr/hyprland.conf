# #######################################################################################

# # AUTOGENERATED HYPRLAND CONFIG.
# # PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf
# Sources
source = ~/.config/hypr/binds.conf
source = ~/.config/hypr/colors.conf
################
### MONITORS ###
################
#monitor = DP-1, 1920x1080, 0x0, 1
# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,0.90


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = pcmanfm
$mainMod = SUPER
#$menu = wofi --show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
# exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
exec-once = udiskie --tray &
exec-once = swappy
exec-once = hypridle
exec-once = xset +dpms
exec-once = xset s 300 300

# Wallpaper
#exec-once = hyprpaper #-c ~/.config/hypr/hyprpaper.conf
exec-once = swww-daemon & # Critical for auto-start
#exec-once = sleep 1 && swww img ~/Pictures/Makima/Makima1.jpg --transition-type wipe && wal -i ~/Pictures/Makima/Makima1.jpg
exec-once = waybar
#exec-once = nwg-dock-hyprland
exec-once = eww daemon
#exec-once = eww open your_widget_name
#exec-once = nwg-dock-hyprland -i 10 -l bottom
#exec-once = hypr-dock
exec-once = wl-paste --watch cliphist store &
exec-once = fcitx5
#exec-once = hyprctl dispatch layout dwindle
#exec-once = sleep 2 && conky -c ~/.config/conky/conky.conf



#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
#env = QT_QPA_PLATFORMTHEME,qt6ct
# Environment variables
env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORMTHEME,qt6ct
env = GDK_BACKEND,wayland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = XDG_CURRENT_DESKTOP,Hyprland

# For better font rendering
env = FREETYPE_PROPERTIES,truetype:interpreter-version=40
env = QT_FONT_DPI,96

# Disable fractional scaling if you have it enabled (can cause blurry fonts)
monitor = ,preferred,auto,1


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 4
    gaps_out = 8

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    #col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    #col.inactive_border = rgba(595959aa)
    col.active_border = $foreground#rgba(ffffffff) 45deg #rgba(ad9b97ff) 45deg
    col.inactive_border = $color0 #rgba(00000025)
    

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Default split direction
#    default_split_direction = right

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle #master #dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 15
    rounding_power = 5

    # Change transparency of focused and unfocused windows
    active_opacity = 0.950
    inactive_opacity = 0.875
    # Kitty for cmatrix
    #windowrulev2 = opacity 1.0 1.0,class:^(kitty)$
    shadow {
        #enabled = true
        #range = 4
        #render_power = 3
        #color = rgba(00000088)
	#color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1
	new_optimizations = true
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, almostLinear #, fade #easeOutQuint
    animation = windows, 1, 6, almostLinear, gnomed #slide # #easeOutQuint
    animation = windowsIn, 1, 6, easeOutQuint, gnomed  #slide #popin 5% 
    animation = windowsOut, 1, 6, easeOutQuint, gnomed  #slide #popin 5% #, fade 
    animation = fadeIn, 1, 2.5, almostLinear
    animation = fadeOut, 1, 2.2, almostLinear
    animation = fade, 1, 4.5, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 7.0, almostLinear, slidefade #slidevert #slidefadevert #slide #fade
    animation = workspacesIn, 1, 6.0, almostLinear, slidefade #slidevert #slidefadevert #slide #fade
    animation = workspacesOut, 1, 7.0, almostLinear, slidefade #slidevert #slidefadevert #slide #fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}
# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {    
	force_default_wallpaper = 0 
	# Set to 0 or 1 to disable the anime mascot wallpapers
    	disable_hyprland_logo = true 
	# If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = 
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        # Reverse Scroll on
        natural_scroll = true
        # Double tap touchpad = right click mouse
        tap-to-click = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    #workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$
#windowrulev2 = float, size 800 600, class:Thunar


# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
